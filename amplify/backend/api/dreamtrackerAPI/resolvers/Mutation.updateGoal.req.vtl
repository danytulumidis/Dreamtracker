#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateGoalInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateGoalInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Goal SET $update WHERE goalID=$ctx.args.updateGoalInput.goalID", "SELECT * FROM Goal WHERE goalID=$ctx.args.updateGoalInput.goalID"]
}