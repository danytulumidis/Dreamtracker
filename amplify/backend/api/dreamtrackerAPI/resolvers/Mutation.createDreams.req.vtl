#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createDreamsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createDreamsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Dreams $colStr VALUES $valStr", "SELECT * FROM Dreams WHERE id=$ctx.args.createDreamsInput.id"]
}