#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createDreamInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createDreamInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Dream $colStr VALUES $valStr", "SELECT * FROM Dream WHERE dreamID=LAST_INSERT_ID()"]
}